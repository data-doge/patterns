<%= form_with(model: giftrocket, local: true) do |form| %>
  <% if giftrocket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(giftrocket.errors.count, "error") %> prohibited this giftrocket from being saved:</h2>

      <ul>
      <% giftrocket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
