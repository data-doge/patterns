<%= form_with(model: cash_card, local: true) do |form| %>
  <% if cash_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cash_card.errors.count, "error") %> prohibited this cash_card from being saved:</h2>

      <ul>
      <% cash_card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
