<h1>New Pool</h1>

<%= form_tag cart_index_path, id: 'new_cart' do |f| %>
  <% if @cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cart.errors.count, "error") %> prohibited this pool from being saved:</h2>

      <ul>
      <% @cart.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label>Name</label>
    <%= text_field_tag :name %>
  </div>
  <div class="field">
    <label>description</label>
    <%= text_field_tag :description %>
  </div>
  <div class="actions">
    <%= submit_tag %>
  </div>
<% end %>


<%= link_to 'Back', cart_index_path %>
