// if the current page is the pool page, refresh, otherwise, just change the things.
// how do we handle the add/remove buttons? maybe refresh the whole page then too?
// huh.

$('#cart_mini_view').html("<%= j render 'cart/mini_view', people: current_cart.people %>");

$('.cart-size').html('<%= current_cart.people.size %>');
$('.current_cart').html('<%= link_to current_cart.name, cart_index_path(current_cart) %>');

$( document ).ajaxComplete(function() {
  $(document).trigger('turbolinks:load');
});

