$('#reward-<%= @reward.id%>').remove();
if ($('#reward_total').length) {
  <% @reward.person.reload %>
  $('#reward_total').html("<%= humanized_money_with_symbol @reward.person.reward_total %>");
}

$('#dynamic-invitation-panel').load('/sessions/' + research_session_id + '/invitations_panel.html');
