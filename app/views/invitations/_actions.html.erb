<% invitation.aasm.events(permitted:true).each do |e| %>
            <%= button_to e.name,
                  research_session_invitations_path(@research_session,invitation,e.name),
                  method: :post,
                  remote: true,
                  class: "btn btn-mini btn-block dynamic-invitations" %>
<% end %>
